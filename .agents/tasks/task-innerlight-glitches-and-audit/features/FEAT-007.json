{
  "id": "FEAT-007",
  "type": "fix",
  "description": "PAGE TRANSLATION coverage (added at the founder's explicit request during this session: 'making sure all of my pages translate properly into the appropriate language'). This is also FOUNDER_WORKLIST.md standing item 'Full page-translation bake (needs API key; 121 pairs; then COMPLETE marker)'. Verify and complete everything about the translation system that can be done WITHOUT the ANTHROPIC_API_KEY (which lives only in the founder's Render dashboard, not in this sandbox), so that the moment the key is present the bake is a one-step, verified operation. IMPORTANT HONEST CONSTRAINT: the actual per-page bake (tools/bake_page_i18n.py) REQUIRES ANTHROPIC_API_KEY and CANNOT be completed in this sandbox. Do NOT fabricate translations (Immutable Principle 13 — no hallucinating; and LANGUAGE_BUILD_BRIEF.md section L requires native review, never presenting machine output as verified). Instead, make the plumbing correct and provide the founder a plain-language, non-technical explanation of the one decision/action he needs to take.",
  "status": "completed",
  "steps": [
    "Read LANGUAGE_BUILD_BRIEF.md, tools/bake_page_i18n.py, and tools/check_lang_parity.py (page_coverage() function) fully to understand the intended translation system. The system: per-page baked JSON in core/i18n_pages/{lang}_{page}.json for 11 languages x 11 pages = 121 pairs; a core/i18n_pages/COMPLETE marker arms strict mode; runtime self-translation is the bootstrap fallback.",
    "VERIFY THE INFRASTRUCTURE (what can be done without the API key). Run python tools/check_lang_parity.py and confirm: (a) every in-app dictionary carries all 11 languages with equal key counts (parity gate passes); (b) the info-page picker and info chrome carry all 11 languages; (c) page_coverage() reports the current baked count (expected 0/121 in this sandbox, bootstrap mode). Confirm the server-side supported-language gates and loaders described in LANGUAGE_BUILD_BRIEF.md sections A-K are actually wired for all languages the app advertises, so that a visitor selecting any advertised language does NOT silently fall back to English at the page level beyond the documented runtime self-translation. If any advertised language is missing from a loader/supported-check/picker (a real bug that would cause English to leak), FIX that wiring here — this is code-only and needs no API key.",
    "Confirm the runtime self-translation fallback path works so that, until the bake is done, a non-English visitor still gets translated content rather than raw English where the system intends to self-translate. Do not change the crisis/safety strings' existing localized handling.",
    "Do NOT run tools/bake_page_i18n.py to fabricate files and do NOT create the COMPLETE marker: without ANTHROPIC_API_KEY it cannot produce real translations, and faking them would violate Principle 13 and the native-review requirement. If ANTHROPIC_API_KEY happens to be set in the environment, you MAY run the bake and commit the produced JSON, but then explicitly flag every set as 'ready, pending native review' per LANGUAGE_BUILD_BRIEF.md section L, and only create COMPLETE if the founder's process calls for it (it does not in this sandbox — leave COMPLETE out).",
    "Write a short plain-language section into the security/status hand-off for the founder (either append a clearly-titled section to SECURITY_AUDIT_FINDINGS.md if it exists, or create TRANSLATION_STATUS.md in the repo root) explaining in non-technical terms: what works today (the app translates pages on the fly), what is not yet permanent (the 121 professionally-baked page translations), that completing it needs the translation key to be present when the build runs, and that the founder's only action is a dashboard decision (confirm the key is set on Render) — never a command. Spell out any acronym on first use (API = Application Programming Interface).",
    "Run the full validation gate after any code change."
  ],
  "acceptance_criteria": [
    "python tools/check_lang_parity.py passes (dictionary parity + info-page surfaces) and reports the current page-coverage count honestly.",
    "Every language the app advertises in its picker is actually wired through the server-side loaders/supported-language checks (no advertised language silently 404s/falls back to English at the page level due to missing wiring); any such wiring bug found is fixed.",
    "No fabricated/hallucinated translations were committed and the COMPLETE strict marker was NOT falsely created.",
    "A plain-language status note for the founder exists (in SECURITY_AUDIT_FINDINGS.md or TRANSLATION_STATUS.md) describing what works, what remains, and the single non-technical action needed (confirm the API key on Render), with acronyms spelled out.",
    "The full validation gate (py_compile, check_frontend 0 failures, lang parity, smoke) passes."
  ],
  "verification": [
    "cd /projects/sandbox/innerlight && python tools/check_lang_parity.py",
    "python -m py_compile core/axiom_harmony_unified_app.py && echo COMPILE_OK",
    "python check_frontend.py core/axiom_harmony_unified_app.py",
    "test -f SECURITY_AUDIT_FINDINGS.md || test -f TRANSLATION_STATUS.md && echo STATUS_NOTE_EXISTS"
  ],
  "blocked_reason": null,
  "findings": "PARITY GATE: python tools/check_lang_parity.py exits 0. (a) Every in-app i18n dictionary carries all 11 languages with equal key counts: 'language parity: every dictionary carries every language (5 dicts x 11 languages)'. (b) Info-page picker + info chrome carry all 11 languages: 'info-page surfaces: picker + chrome carry all 11 languages'. (c) page_coverage(): 'page translation coverage: 0/121 baked' — bootstrap mode (expected in this sandbox; no ANTHROPIC_API_KEY, so no real bake possible). No COMPLETE marker present, so it stays exit 0 (not strict fail).\n\nWIRING AUDIT (code-only, no key). Line anchors in LANGUAGE_BUILD_BRIEF.md had drifted (grep_search fails on this 1.3MB file's long lines; used bash grep). Findings:\n - Info pages are correctly wired for all 11 advertised languages: _PAGE_LANGS = (es,zh,hi,pa,bn,tl,to,sw,am,ha,ru); _info_lang() accepts en + all 11 and maps unknown->en; _info_page() serves baked/committed/cached translation if present else serves English instantly and fires _page_i18n_kick() (runtime self-translation, QE-judged, cached) when the key is set; _INFO_CHROME carries all 11; the info-page picker advertises all 11 + en; the gate switcher (data-langbtn) advertises all 11 + en. No leak here.\n - BUG FOUND + FIXED: _load_handoff_i18n() iterated only 7 languages ('es','zh','hi','pa','bn','tl','to') while the picker and _info_lang()/_PAGE_LANGS advertise 11. sw/am/ha/ru were absent from the handoff loader, so a native-reviewed i18n_handoff_{sw,am,ha,ru}.json could never load even once authored. Changed the loop to iterate _PAGE_LANGS (all 11), used setdefault, and treat FileNotFoundError as the expected not-yet-authored case (quiet) while still logging real errors. Verified _HANDOFF_I18N now covers all 11 advertised langs (set(_PAGE_LANGS) <= keys == True). Deliberately did NOT add runtime self-translation to handoff pages: they contain deterministic crisis/safety numbers that must never be machine-translated at runtime (briefing + Immutable Principles), so they fall back to English until a native-reviewed handoff file exists — the loader is now ready for those files.\n\nRUNTIME FALLBACK CONFIRMED via app.test_client(): /about?lang=sw, /faq?lang=ru, /handoff/clinical?lang=am, /handoff/legal?lang=to, /?lang=hi all return 200 (English served instantly; kick is a no-op with no key). No errors.\n\nNO FABRICATION: did not run tools/bake_page_i18n.py, created no translation files, created NO core/i18n_pages/COMPLETE marker (no baked dir at all). Only es/zh i18n_pages_*.json and i18n_handoff_*.json exist (pre-existing). Immutable Principle 13 and LANGUAGE_BUILD_BRIEF.md section L (native review) respected.\n\nFOUNDER NOTE: appended a plain-language 'Page Translations — Status' section to SECURITY_AUDIT_FINDINGS.md (repo root) — acronyms spelled out (API, CI, Render, Anthropic/Claude), states what works today (on-the-fly runtime self-translation), what is not permanent (the 121 professionally-baked pairs, currently 0/121), that finishing needs the translation key present at build time, that the founder's ONLY action is a dashboard check (confirm the Anthropic API key is set on Render) never a command, and how the keyless-for-founder flow completes it (the running app bakes in the background, or a keyed CI / orchestrator bake run produces all 121 in one pass, followed by native review, only then the COMPLETE marker).\n\nGATE (all pass): py_compile core/*.py OK (no SyntaxWarning); check_frontend 38 script blocks / 0 failures; check_lang_parity exit 0; check_crisis_safety all laws hold; check_hybrid_fallback live-primary + observable fallback pass."
}
