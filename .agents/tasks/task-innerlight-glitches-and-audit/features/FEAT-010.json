{
  "id": "FEAT-010",
  "type": "fix",
  "description": "PHASE B — Land the founder-APPROVED warm friend/chaplain/grief-companion fallback crisis lines from .agents/tasks/task-innerlight-glitches-and-audit/CRISIS_VOICE_SAMPLES.md, replacing the clinical/repetitive local fallback lines everywhere they are emitted, across all severity tiers and the 'learning' lines. Safety wording (988, 911, HOME to 741741, the loved-one holding law, no-dead-end) stays EXACTLY intact. These lines are the LOCAL FALLBACK shown only when live Claude is unavailable/over-budget; the samples file was read out loud and the founder approved the warm voice direction. The exact 'before' strings live in core/crisis_response_core.py (public_response for critical ~line 218, high ~line 251, moderate ~line 278, low ~line 306) and core/innerlight_learning_module.py (_response_for_state critical ~line 267, high ~line 269). The four sample tiers map to critical/high/moderate/low crisis tiers; the two learning lines map to the module's critical/high.",
  "status": "completed",
  "steps": [
    "Read .agents/tasks/task-innerlight-glitches-and-audit/CRISIS_VOICE_SAMPLES.md carefully — the five approved 'After' rewrites (four crisis tiers plus the two short learning lines) are the authoritative target text.",
    "In core/crisis_response_core.py: replace the four public_response strings with the approved 'After' warm text, tier-by-tier. Critical (~line 218) -> sample #1 'After'; high (~line 251) -> sample #2 'After'; moderate (~line 278) -> sample #3 'After'; low/steady_checkin (~line 306) -> sample #4 'After'. Preserve the existing {address} name-prefixing pattern where present and keep it grammatical with the new warm text. KEEP the 988/call-or-text and 911/emergency wording present in the critical tier — the approved #1 'After' already contains '988 (call or text)'; ensure 911/emergency-services immediacy is not lost (retain an emergency-services line in next_steps). Do NOT change next_steps, questions, provider_focus, sound_mode, needs_immediate_support, or any structural field — only the spoken public_response prose.",
    "In core/innerlight_learning_module.py _response_for_state (~line 265-272): replace the critical line (~267) with the approved learning 'After (critical)' text and the high line (~269) with the approved learning 'After (high)' text. Keep the trailing emotion_line concatenation and the moderate/low lines as they are unless they read as stock — if lightly warming moderate/low, keep them plain and non-repetitive and do not add safety-number changes.",
    "Check core/conversation_engine.py and app.py support_response (~line 341) for any additional clinical fallback lines that a real user could see in the fallback path; where they duplicate the disliked clinical tone, warm them consistent with the approved voice WITHOUT changing safety numbers or inventing features (Principle 13). Do not touch the live Claude SYSTEM_PROMPT here (that is FEAT-011).",
    "Verify no stock-line duplication is introduced: the warm lines must not become a new repeated script the returning user notices. They are fallback only; keep them varied per tier as written. Confirm 988/911/741741 remain present and reachable in every tier that had them before.",
    "Run the full validation gate; run tools/check_crisis_safety.py to confirm crisis-safety regressions still pass with the new wording."
  ],
  "acceptance_criteria": [
    "All four crisis-tier public_response strings in core/crisis_response_core.py match the approved 'After' text from CRISIS_VOICE_SAMPLES.md.",
    "The two learning lines in core/innerlight_learning_module.py match the approved learning 'After' text.",
    "988 (call and text), 911/emergency services, and HOME to 741741 remain present wherever they were before; no safety wording removed or weakened.",
    "The loved-one holding behavior and no-dead-end structure are untouched.",
    "tools/check_crisis_safety.py passes.",
    "The full validation gate (py_compile, check_frontend 0 failures, lang parity, smoke) passes."
  ],
  "verification": [
    "cd /projects/sandbox/innerlight && python -m py_compile core/crisis_response_core.py core/innerlight_learning_module.py core/axiom_harmony_unified_app.py && echo COMPILE_OK",
    "python tools/check_crisis_safety.py",
    "python check_frontend.py core/axiom_harmony_unified_app.py",
    "python tools/check_lang_parity.py",
    "python - <<'PY'\nimport sys; sys.path.insert(0,'core')\nimport crisis_response_core as c\ne=c.__dict__\nfrom crisis_response_core import CrisisEngine if hasattr(c,'CrisisEngine') else None\nPY"
  ],
  "blocked_reason": null,
  "findings": "COMPLETED. Landed the founder-approved warm friend/chaplain fallback voice from CRISIS_VOICE_SAMPLES.md.\n\nEXACT LINES CHANGED:\n- core/crisis_response_core.py evaluate(): replaced the four public_response strings only (all other structural fields untouched):\n  * critical (~line 217-224) -> sample #1 'After' ('...okay. I'm right here, and I'm not going anywhere...988 (call or text) puts a real person with you instantly...'). Kept {address} name prefix (lowercased 'okay' for grammar). 988 (call or text) retained inside the prose.\n  * high (~line 251-255) -> sample #2 'After'.\n  * moderate (~line 279-282) -> sample #3 'After'.\n  * low/steady_checkin (~line 306-309) -> sample #4 'After'.\n- core/innerlight_learning_module.py _response_for_state (~line 265-268): critical line -> approved learning 'After (critical)' ('I'm here. Stay with me...'); high line -> approved learning 'After (high)' ('I'm listening. We'll go gentle and slow...'). Trailing + emotion_line concatenation kept. moderate/low lines left as-is (plain, non-repetitive, no safety numbers).\n\nNOT CHANGED (verified): next_steps, questions, provider_focus, sound_mode, needs_immediate_support, needs_investigation, investigation fields. Live Claude SYSTEM_PROMPT in comprehension_engine.py untouched (that is FEAT-011). conversation_engine.py and axiom_harmony_unified_app.py had no clinical fallback duplicates to warm (they already use human language; no support_response() exists there).\n\nSAFETY CONFIRMED INTACT:\n- 988 (call and text): present in critical public_response ('988 (call or text)') AND critical next_steps ('call or text 988 in the U.S.') AND high next_steps. human_help fixture still carries lifeline='988'.\n- 911 / emergency services: critical next_steps retains 'call or text 988 in the U.S. or call emergency services now'. check_crisis_safety confirms the 911 door fixture.\n- HOME to 741741: check_crisis_safety confirms human_help text_line still carries 741741 for both loved-one and no-provider paths.\n- Loved-one holding law + no-dead-end structure: check_crisis_safety all green (witness_grief holding, availability-driven provider gating unchanged).\n\nVALIDATION GATE (all pass): py_compile (no SyntaxWarning) on crisis_response_core.py, innerlight_learning_module.py, axiom_harmony_unified_app.py; tools/check_crisis_safety.py all fatal laws hold; check_frontend.py 38 script blocks / 0 failures; tools/check_lang_parity.py exit 0 (page-coverage MISSING list is the pre-existing bootstrap baseline, not a regression); smoke test app.test_client() POST /api/checkin with ANTHROPIC_API_KEY unset returns 200 with warm fallback text and 988/741741 fixtures present across all four tiers. Direct CrisisResponseCore.evaluate() confirms the warm prose surfaces per-tier and stays varied (no repeated script)."
}
